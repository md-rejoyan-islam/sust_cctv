import Zones from "@/components/zone";

export const metadata = {
  title: "Zones",
  description: "Manage and monitor all zones in the system",
};

const Page = () => {
  return <Zones />;
};

export default Page;
