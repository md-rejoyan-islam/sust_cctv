import Cameras from "@/components/cameras";

export const metadata = {
  title: "Cameras",
  description: "Manage and monitor all cameras in the system",
};

const Page = () => {
  return <Cameras />;
};

export default Page;
